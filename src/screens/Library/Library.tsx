import styled from 'styled-components';

const Library = () => {
  return (
    <Wrapper>
      <h2>Library</h2>
    </Wrapper>
  );
};

const Wrapper = styled.main`
  padding: 5px 5px;
`;

export default Library;
