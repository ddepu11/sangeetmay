import styled from 'styled-components';

const Account = () => {
  return (
    <Wrapper>
      <h2>Account</h2>
    </Wrapper>
  );
};

const Wrapper = styled.main`
  padding: 5px 5px;
`;

export default Account;
