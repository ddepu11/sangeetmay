import styled from 'styled-components';

const Search = () => {
  return (
    <Wrapper>
      <h2>Search</h2>
    </Wrapper>
  );
};

const Wrapper = styled.main`
  padding: 5px 5px;
`;

export default Search;
